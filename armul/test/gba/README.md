# Tests from `gba-tests`

The tests in this directory are adapted from [jsmolka's `gba-tests`](https://github.com/jsmolka/gba-tests), an MIT-licensed test suite for GameBoy Advance emulators, which use the same ARM7TDMI processor.
